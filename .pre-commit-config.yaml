# Pre-commit hooks for unified-llm
# Runs Ruff linting and formatting locally before commits
# to catch issues early and keep CI green.

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.10
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
